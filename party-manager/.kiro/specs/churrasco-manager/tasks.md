# Implementation Plan

- [x] 1. Set up project structure and configuration
  - Initialize Next.js 14 project with App Router, TypeScript, Tailwind CSS, and ESLint
  - Install dependencies: `@supabase/supabase-js`, `@supabase/ssr`, `fast-check`, `vitest`, `framer-motion`, `qrcode.react`
  - Configure Supabase client (lib/supabase/client.ts and lib/supabase/server.ts)
  - Configure Vitest in vitest.config.ts
  - Set up environment variables template (.env.local.example) for NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY
  - Create base layout with dark theme (app/layout.tsx) and global styles
  - _Requirements: 9.1, 9.2_

- [x] 2. Implement data models, validators, and serialization
  - [x] 2.1 Create TypeScript interfaces and serialization utilities
    - Define all data model interfaces in `src/lib/models/types.ts` (User, Event, Payment, Comment, Photo, ChecklistItem, RankingEntry)
    - Implement `serializeModel` and `deserializeModel` functions in `src/lib/models/serialization.ts` with type validators
    - _Requirements: 11.1, 11.2_
  - [x] 2.2 Write property test: serialization round-trip
    - **Property 14: Data model serialization round-trip**
    - **Validates: Requirements 11.1, 11.2**
  - [x] 2.3 Implement auth validation functions
    - Create `src/lib/validators/auth.ts` with `validateEmail` and `validatePassword` functions
    - _Requirements: 1.3_
  - [x] 2.4 Write property test: input validation rejects invalid credentials
    - **Property 1: Input validation rejects invalid credentials**
    - **Validates: Requirements 1.3**
  - [x] 2.5 Implement payment validation functions
    - Create `src/lib/validators/payment.ts` with `validatePaymentAmount` function
    - _Requirements: 3.5_
  - [x] 2.6 Write property test: payment amount validation
    - **Property 6: Payment amount validation**
    - **Validates: Requirements 3.5**
  - [x] 2.7 Implement file upload validation
    - Create `src/lib/validators/upload.ts` with `validateFileUpload` function (type and size checks)
    - _Requirements: 5.3_
  - [x] 2.8 Write property test: file upload validation
    - **Property 9: File upload validation**
    - **Validates: Requirements 5.3**
  - [x] 2.9 Implement checklist item validation
    - Create `src/lib/validators/checklist.ts` with `validateChecklistDescription` function
    - _Requirements: 7.2_
  - [x] 2.10 Write property test: checklist item description validation
    - **Property 12: Checklist item description validation**
    - **Validates: Requirements 7.2**

- [x] 3. Implement core utility functions
  - [x] 3.1 Implement countdown calculation
    - Create `src/lib/utils/countdown.ts` with `calculateCountdown(targetDate, now)` returning `{ days, hours, minutes, seconds }`
    - _Requirements: 2.2_
  - [x] 3.2 Write property test: countdown calculation correctness
    - **Property 2: Countdown calculation correctness**
    - **Validates: Requirements 2.2**
  - [x] 3.3 Implement photo grouping by year
    - Create `src/lib/utils/photos.ts` with `groupPhotosByYear(photos, events)` function
    - _Requirements: 5.4_
  - [x] 3.4 Write property test: photos grouped by event year
    - **Property 10: Photos grouped by event year**
    - **Validates: Requirements 5.4**

- [x] 4. Implement payment and ranking business logic
  - [x] 4.1 Implement payment computation functions
    - Create `src/lib/services/payments.ts` with functions: `computeUserBalance(payments, totalRequired)`, `computeEventTotals(payments, totalRequired)`, `createPaymentRecord(amount, role)` returning payment with correct status
    - _Requirements: 2.3, 3.1, 3.3, 4.4, 4.5, 10.3_
  - [x] 4.2 Write property test: payment consistency invariant
    - **Property 3: Payment consistency invariant**
    - **Validates: Requirements 2.3, 4.5, 10.3**
  - [x] 4.3 Write property test: user balance computation
    - **Property 4: User balance computation**
    - **Validates: Requirements 3.1**
  - [x] 4.4 Write property test: payment creation status by role
    - **Property 5: Payment creation status by role**
    - **Validates: Requirements 3.3, 4.4**
  - [x] 4.5 Implement payment history sorting
    - Add `sortPaymentsByDate(payments)` function to payments service (ascending chronological)
    - _Requirements: 3.4_
  - [x] 4.6 Write property test: payment history chronological order
    - **Property 7: Payment history chronological order**
    - **Validates: Requirements 3.4**
  - [x] 4.7 Implement ranking computation
    - Create `src/lib/services/ranking.ts` with `computeRanking(payments, users)` returning sorted ranking with badge assignment
    - _Requirements: 6.1, 6.2_
  - [x] 4.8 Write property test: ranking sorted with badge assignment
    - **Property 11: Ranking sorted with badge assignment**
    - **Validates: Requirements 6.1, 6.2**

- [x] 5. Implement comment and checklist logic
  - [x] 5.1 Implement comment ordering
    - Create `src/lib/services/comments.ts` with `sortCommentsReverseChronological(comments)` function
    - _Requirements: 5.1_
  - [x] 5.2 Write property test: comments reverse chronological order
    - **Property 8: Comments reverse chronological order**
    - **Validates: Requirements 5.1**
  - [x] 5.3 Implement checklist toggle logic
    - Create `src/lib/services/checklist.ts` with `toggleChecklistItem(item)` function
    - _Requirements: 7.3_
  - [x] 5.4 Write property test: checklist toggle round-trip
    - **Property 13: Checklist toggle round-trip**
    - **Validates: Requirements 7.3**

- [x] 6. Checkpoint - Make sure all tests are passing
  - Ensure all tests pass, ask the user if questions arise.

- [x] 7. Set up Supabase database schema and RLS
  - Create SQL migration file `supabase/migrations/001_initial_schema.sql` with all tables (users, eventos, pagamentos, comentarios, fotos, checklist_items)
  - Define RLS policies: users see own data, admins see all data
  - Create Supabase storage bucket "photos" with upload policies for authenticated users
  - _Requirements: 10.1, 10.2_

- [x] 8. Implement authentication flow
  - [x] 8.1 Create Supabase auth helpers
    - Create `src/lib/supabase/auth.ts` with `signUp`, `signIn`, `signOut`, `getSession`, `getUserProfile` functions
    - _Requirements: 1.1, 1.2_
  - [x] 8.2 Create auth middleware
    - Create `src/middleware.ts` to protect routes and redirect unauthenticated users to login
    - _Requirements: 1.4_
  - [x] 8.3 Build login/register page
    - Create `src/app/page.tsx` with login and registration forms, validation, and error display
    - Use auth validators from step 2.3
    - _Requirements: 1.1, 1.2, 1.3_

- [x] 9. Build dashboard page
  - [x] 9.1 Create dashboard layout and event info display
    - Create `src/app/dashboard/page.tsx` showing event name, date, time, location
    - Handle "no active event" state with appropriate message
    - _Requirements: 2.1, 2.4_
  - [x] 9.2 Add CountdownTimer component
    - Create `src/components/CountdownTimer.tsx` using countdown utility from step 3.1
    - Use client-side interval for real-time updates
    - _Requirements: 2.2_
  - [x] 9.3 Add financial summary section
    - Display total required, total collected, and remaining amount using payment computation from step 4.1
    - _Requirements: 2.3_
  - [x] 9.4 Add RankingList component with badge
    - Create `src/components/RankingList.tsx` using ranking computation from step 4.7
    - Display "Pagador Oficial do Churras" badge for top contributor
    - _Requirements: 6.1, 6.2_
  - [x] 9.5 Add SpotifyEmbed component
    - Create `src/components/SpotifyEmbed.tsx` rendering Spotify iframe when playlist URL exists
    - _Requirements: 8.1, 8.2_

- [x] 10. Build payments page
  - [x] 10.1 Create payments page with user balance
    - Create `src/app/payments/page.tsx` showing total paid and remaining balance
    - _Requirements: 3.1_
  - [x] 10.2 Add PaymentForm and QRCodePix components
    - Create `src/components/PaymentForm.tsx` with amount input and validation
    - Create `src/components/QRCodePix.tsx` with simulated QR code
    - _Requirements: 3.2, 3.3, 3.5_
  - [x] 10.3 Add payment history list
    - Display chronological list of user payments with amount, date, and status
    - _Requirements: 3.4_

- [x] 11. Build admin page
  - [x] 11.1 Create admin page with route protection
    - Create `src/app/admin/page.tsx` with admin-only access check
    - _Requirements: 1.4_
  - [x] 11.2 Add event management form
    - Create event creation/editing form with fields: name, date, time, location, total required, Spotify URL
    - _Requirements: 4.1, 4.2, 8.1_
  - [x] 11.3 Add user management and manual payment section
    - Display all users with balances, add/remove payment functionality
    - _Requirements: 4.3, 4.4, 4.5_

- [x] 12. Build social features
  - [x] 12.1 Add CommentFeed component
    - Create `src/components/CommentFeed.tsx` with comment form and reverse chronological feed
    - Integrate into dashboard page
    - _Requirements: 5.1_
  - [x] 12.2 Add PhotoUpload and PhotoGallery components
    - Create `src/components/PhotoUpload.tsx` with file validation from step 2.7
    - Create `src/components/PhotoGallery.tsx` with year-grouped responsive grid
    - Create `src/app/gallery/page.tsx`
    - _Requirements: 5.2, 5.3, 5.4_

- [x] 13. Build checklist page
  - Create `src/app/checklist/page.tsx` with ChecklistManager component
  - Admin can add/delete items, all users can toggle completion
  - Use validation from step 2.9 and toggle logic from step 5.3
  - _Requirements: 7.1, 7.2, 7.3_

- [x] 14. Add animations and polish UI
  - Add Framer Motion page transitions and component animations (duration < 500ms)
  - Ensure responsive layout across mobile, tablet, and desktop
  - Polish dark theme consistency across all pages
  - Add navigation bar with links to all pages
  - _Requirements: 9.1, 9.2, 9.3_

- [x] 15. Final Checkpoint - Make sure all tests are passing
  - Ensure all tests pass, ask the user if questions arise.
